# astroinformatics-minimal

Minimal Docker image with the astroinformatics stacks.

This image is based on `Ubuntu 16.04` and `python2.7`, it also does not
contain a work directory or a user. It should be used as a base image.
